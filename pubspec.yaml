name: jalali_table_calendar_plus
description: "A Package to use Jalali calendar in table view and also a Dialog to peak date"
version: 1.1.3
homepage:  https://github.com/Azare77/jalali_table_calendar_plus

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shamsi_date: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0